generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Pembeli {
  id      Int    @id @default(autoincrement())
  nama    String
  pesanan String
}
